import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo (Primary)
  static const Color primary = Color(0xFF007AFF); // A modern, iOS-style blue

  // Màu phụ (Secondary)
  static const Color secondary = Color(0xFF34C759); // Green for success or secondary actions

  // Màu nền (Background)
  static const Color background = Color(0xFFF5F5F7); // A very light gray for depth
  static const Color surface = Colors.white; // For cards, sheets, and other surfaces

  // Màu chữ (Text)
  static const Color textPrimary = Color(0xFF1D1D1F); // Near-black for major text
  static const Color textSecondary = Color(0xFF8A8A8E); // Gray for secondary text

  // Màu cho các thành phần khác
  static const Color error = Color(0xFFFF3B30); // Red for errors
  static const Color divider = Color(0xFFE5E5EA); // Light gray for dividers and borders
}
